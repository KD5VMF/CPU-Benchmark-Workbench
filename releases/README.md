# Releases Folder

This folder is only a placeholder in the repository template.

For public distribution, it is usually better to upload built zip files to **GitHub Releases** instead of storing every binary archive in the repository itself.
